/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['www.artmajeur.com'],
  },
  // Adicione outras configurações necessárias
}

module.exports = nextConfig